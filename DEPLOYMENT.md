# راهنمای استقرار دستیار هوشمند Niyara در پلتفرم ابری لیارا (Liara PaaS)

این سند مراحل گام‌به‌گام استقرار پروژه **Niyara** (دستیار هوشمند مستندات لیارا) را در پلتفرم ابری لیارا توضیح می‌دهد.

---

## ۱. پیش‌نیازها

۱. حساب کاربری در [پلتفرم ابری لیارا](https://liara.ir)
۲. نصب [Liara CLI](https://docs.liara.ir/cli/install/) یا استفاده از پنل وب کاربری لیارا
۳. کلید دسترسی سرویس هوش مصنوعی لیارا (`LIARA_AI_API_KEY`)

---

## ۲. مشخصات و پیکربندی برنامه

این پروژه با فریم‌ورک **Next.js 14** و زبان **TypeScript** توسعه یافته و برای استقرار روی پلتفرم **NodeJS** لیارا پیکربندی شده است.

### فایل `liara.json`:
```json
{
  "platform": "node",
  "node": {
    "version": "20"
  },
  "port": 3000
}
```

### اسکریپت‌های اجرا در `package.json`:
- `npm run build`: ساخت کدهای بهینه‌شده پروداکشن Next.js
- `npm run start`: اجرای سرور پروداکشن روی پورت تعیین‌شده (`PORT=3000`)
- `npm run build:embeddings`: تولید ایندکس امبدینگ و جستجوی لغوی

---

## ۳. متغیرهای محیطی (Environment Variables)

پیش از استقرار برنامه در لیارا، متغیرهای محیطی زیر را در بخش **تنظیمات برنامه > متغیرهای محیطی** در پنل لیارا وارد نمایید:

| نام متغیر | مقدار نمونه / توضیحات | الزامی |
| :--- | :--- | :--- |
| `LIARA_AI_API_KEY` | کلید دسترسی سرویس AI لیارا (`eyJ...`) | بله |
| `LIARA_AI_BASE_URL` | `https://ai.liara.ir/v1` | خیر (پیش‌فرض تنظیم است) |
| `LLM_MODEL` | `openai/gpt-4o-mini` | خیر (پیش‌فرض تنظیم است) |
| `EMBEDDING_MODEL` | `intfloat/multilingual-e5-large` | خیر (پیش‌فرض تنظیم است) |
| `NODE_ENV` | `production` | بله |
| `PORT` | `3000` | بله |

---

## ۴. روش‌های استقرار

### روش اول: استقرار با Liara CLI (پیشنهادی)

۱. ترمینال را در پوشه پکیج استقرار (`liara-ai-assistant-redy-to-deploy`) باز کنید.
۲. با دستور زیر وارد حساب کاربری لیارا شوید:
   ```bash
   liara login
   ```
3. برای استقرار برنامه، دستور زیر را اجرا نمایید:
   ```bash
   liara deploy --app <نام_برنامه_شما> --platform node --port 3000
   ```

### روش دوم: استقرار از طریق پنل وب یا Git

۱. در پنل لیارا یک برنامه جدید از نوع پلتفرم **NodeJS (Node.js 20)** بسازید.
۲. متغیرهای محیطی ذکرشده را در تب **متغیرها** ثبت کنید.
۳. محتویات پوشه `liara-ai-assistant-redy-to-deploy` را به مخزن گیت متصل کنید یا از طریق آپلود فایل/CLI مستقر نمایید.

---

## ۵. تولید و بروزرسانی داده‌های امبدینگ (در صورت نیاز)

پکیج استقرار به صورت پیش‌فرض شامل داده‌های پردازش‌شده در `data/embeddings.json` و `data/lexical-index.json` است. در صورت نیاز به بازتولید ایندکس‌ها:

```bash
npm run build:embeddings:force
```

این فرایند با اعتبارسنجی امن و اتمیک (Atomic File Replacement) فایل موقت `embeddings.tmp.json` را تولید کرده و تنها پس از تایید کامل داده‌ها جایگزین می‌کند.

---

## ۶. بررسی سلامت برنامه (Health Check)

پس از استقرار، می‌توانید وضعیت سرویس را از طریق اندپوینت زیر بررسی نمایید:

```
GET https://<your-app-domain>.liara.run/api/health
```

پاسخ مورد انتظار:
```json
{
  "status": "ok",
  "timestamp": "2026-08-22T..."
}
```
